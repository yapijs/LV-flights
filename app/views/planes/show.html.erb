<div class="plane_name">
  <h2>
    Plane number
    <%= @plane.icao24 %>
  </h2>
</div>

<div>
  <% if @plane.flights.count == 0 %>

    <h3>Select the interval, where planes, which have been in Riga, have been to:</h3> 
    <%= form_with model: @plane, method: :post do |form| %>
      <%= form.label :start, "Start date:"%>
      <%= form.date_field :start %>
      <%= form.label :end, "End date:"%>
      <%= form.date_field :end %>
      <br>
      <%= form.submit "Request data" %>
    <% end %>
  <% end %>
</div>  
  
</p>

<%= render @plane.flights %>

<%= link_to 'Back', planes_path %>
